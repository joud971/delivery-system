<x-app-layout>
@section('page_title','الإشعارات')
<div class="mx-auto max-w-6xl"><div class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div class="mb-5 flex flex-col justify-between gap-3 sm:flex-row sm:items-center"><div><div class="text-sm text-orange-500">المركز</div><h1 class="text-2xl font-black">الإشعارات</h1><p class="mt-1 text-sm text-slate-500">عدد غير المقروء: {{ $unreadCount }}</p></div>@if($unreadCount)<form method="POST" action="{{ route('notifications.mark-all-read') }}">@csrf<button class="rounded-xl border border-slate-200 bg-white px-4 py-2.5 text-sm font-bold">تحديد الكل كمقروء</button></form>@endif</div><div class="space-y-3">@forelse($notifications as $notification)<a href="{{ route('notifications.show',$notification) }}" class="flex items-center justify-between gap-4 rounded-2xl border border-slate-200 p-4 transition hover:border-orange-200 hover:bg-orange-50/30"><div class="min-w-0"><div class="flex items-center gap-2"><span class="font-bold">{{ $notification->title }}</span>@if(!$notification->is_read)<span class="rounded-full bg-orange-50 px-2 py-0.5 text-[10px] font-bold text-orange-700">جديد</span>@endif</div><p class="mt-1 truncate text-sm text-slate-500">{{ $notification->message }}</p></div><div class="shrink-0 text-xs text-slate-400">{{ $notification->created_at?->diffForHumans() }}</div></a>@empty<p class="py-14 text-center text-slate-400">لا توجد إشعارات.</p>@endforelse</div><div class="mt-5">{{ $notifications->links() }}</div></div></div></x-app-layout>
