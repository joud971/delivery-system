<x-app-layout><div class="dashboard-shell px-4 py-6 md:px-8"><div class="mx-auto max-w-5xl space-y-6"><div class="panel p-6"><form action="{{ route('search') }}" class="flex gap-3"><input name="q" value="{{ $term }}" class="flex-1 rounded-xl border-slate-200" placeholder="ابحث عن طلب أو عميل أو سائق"><button class="rounded-xl bg-slate-900 px-5 font-semibold text-white">بحث</button></form></div><div class="panel p-6"><h1 class="text-xl font-bold">نتائج البحث عن: {{ $term }}</h1><div class="mt-5 space-y-3">@forelse($orders as $order)<a href="{{ route('orders.show', $order) }}" class="flex justify-between rounded-xl border p-3"><span>{{ $order->order_number }} · {{ $order->customer?->name }}</span><span>{{ $order->status }}</span></a>@empty<p class="text-slate-500">لا توجد طلبات مطابقة.</p>@endforelse</div><div class="mt-6 space-y-3">@foreach($customers as $customer)<a href="{{ route('customers.show', $customer) }}" class="block rounded-xl border p-3">عميل: {{ $customer->name }} · {{ $customer->phone }}</a>@endforeach @foreach($drivers as $driver)<a href="{{ route('drivers.show', $driver) }}" class="block rounded-xl border p-3">سائق: {{ $driver->name }} · {{ $driver->phone }}</a>@endforeach</div></div></div></div></x-app-layout>
