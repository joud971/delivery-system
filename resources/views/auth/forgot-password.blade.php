<x-guest-layout><div class="mb-6"><h1 class="text-2xl font-black">استعادة كلمة المرور</h1><p class="mt-2 text-sm text-slate-400">أدخل بريد الحساب وسنرسل لك رابطًا لإعادة تعيين كلمة المرور.</p></div>@if(session('status'))<div class="mb-4 rounded-xl bg-emerald-500/10 p-3 text-sm text-emerald-300">{{ session('status') }}</div>@endif<form method="POST" action="{{ route('password.email') }}" class="space-y-5">@csrf<label class="block text-sm font-bold">البريد الإلكتروني<input name="email" type="email" value="{{ old('email') }}" required class="mt-2 w-full rounded-xl border border-slate-700 bg-slate-950 px-4 py-3 text-white"></label><button class="w-full rounded-xl bg-orange-500 px-4 py-3 font-black">إرسال رابط الاستعادة</button><a href="{{ route('login') }}" class="block text-center text-sm text-slate-400">العودة لتسجيل الدخول</a></form></x-guest-layout>
