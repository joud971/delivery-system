<x-app-layout><div class="dashboard-shell px-4 py-6 md:px-8"><div class="mx-auto max-w-5xl space-y-6"><div class="panel p-6"><h1 class="text-2xl font-bold">المصروفات</h1><form method="POST" action="{{ route('expenses.store') }}" class="mt-5 grid gap-3 md:grid-cols-4">@csrf<input name="category" required placeholder="الفئة" class="rounded-xl border-slate-200"><input name="amount" required type="number" min="0" placeholder="المبلغ" class="rounded-xl border-slate-200"><input name="expense_date" required type="date" value="{{ now()->toDateString() }}" class="rounded-xl border-slate-200"><button class="rounded-xl bg-slate-900 px-4 font-semibold text-white">تسجيل المصروف</button></form></div><div class="panel overflow-x-auto p-6"><table class="min-w-full text-right text-sm"><thead><tr class="border-b"><th class="p-3">الفئة</th><th class="p-3">المبلغ</th><th class="p-3">التاريخ</th><th class="p-3">المسجل</th></tr></thead><tbody>@forelse($expenses as $expense)<tr class="border-b"><td class="p-3">{{ $expense->category }}</td><td class="p-3">{{ number_format($expense->amount, 0) }} ل.س</td><td class="p-3">{{ $expense->expense_date }}</td><td class="p-3">{{ $expense->user?->name }}</td></tr>@empty<tr><td colspan="4" class="p-8 text-center text-slate-500">لا توجد مصروفات.</td></tr>@endforelse</tbody></table>{{ $expenses->links() }}</div></div></div></x-app-layout>
