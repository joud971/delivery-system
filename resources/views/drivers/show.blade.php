<x-app-layout><div class="dashboard-shell px-4 py-6 md:px-8"><div class="mx-auto max-w-4xl space-y-6"><div class="panel p-6"><div class="flex items-center justify-between"><div><p class="text-sm text-slate-500">ملف السائق</p><h1 class="text-2xl font-bold">{{ $driver->name }}</h1></div><a href="{{ route('drivers.edit', $driver) }}" class="rounded-xl bg-slate-900 px-4 py-2 text-sm font-semibold text-white">تعديل</a></div><div class="mt-6 grid gap-4 md:grid-cols-4"><p>الهاتف: {{ $driver->phone }}</p><p>المركبة: {{ $driver->vehicle_type }}</p><p>التقييم: {{ $driver->rating }}</p><p>الحالة: {{ $driver->is_available ? 'متاح' : 'مشغول' }}</p></div></div><div class="panel p-6"><h2 class="text-lg font-bold">آخر الطلبات</h2><div class="mt-4 space-y-3">@forelse($driver->orders as $order)<a href="{{ route('orders.show', $order) }}" class="flex justify-between rounded-xl border p-3"><span>{{ $order->order_number }}</span><span>{{ $order->status }}</span></a>@empty<p class="text-slate-500">لا توجد طلبات مسندة.</p>@endforelse</div></div><div class="panel p-6"><h2 class="text-lg font-bold">جلسات العمل</h2><div class="mt-4 space-y-3">@forelse($driver->workSessions as $session)<div class="flex justify-between rounded-xl border p-3"><span>{{ $session->started_at?->format('Y-m-d H:i') }}</span><span>{{ $session->status }}</span></div>@empty<p class="text-slate-500">لا توجد جلسات عمل.</p>@endforelse</div></div></div></div></x-app-layout>
