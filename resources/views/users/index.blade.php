<x-app-layout>@section('page_title','الحسابات والمستخدمون')<div class="mx-auto max-w-6xl space-y-5"><div class="flex items-end justify-between"><div><div class="text-sm text-orange-500">الإدارة</div><h1 class="text-2xl font-black">الحسابات والمستخدمون</h1><p class="mt-1 text-sm text-slate-500">إدارة الموظفين والأدوار وصلاحيات الوصول.</p></div><a href="{{ route('users.create') }}" class="rounded-xl bg-slate-950 px-4 py-2.5 text-sm font-bold text-white">＋ إضافة حساب</a></div><div class="overflow-x-auto rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><table class="min-w-full text-right text-sm"><thead><tr class="border-b text-slate-400"><th class="pb-3">المستخدم</th><th class="pb-3">اسم المستخدم</th><th class="pb-3">الدور</th><th class="pb-3">الهاتف</th><th class="pb-3">إجراء</th></tr></thead><tbody>@foreach($users as $user)<tr class="border-b"><td class="py-4"><div class="font-bold">{{ $user->name }}</div><div class="text-xs text-slate-400">{{ $user->email }}</div></td><td class="py-4 font-semibold">{{ $user->username }}</td><td class="py-4">{{ $user->getRoleNames()->join('، ') }}</td><td class="py-4">{{ $user->phone ?: '—' }}</td><td class="py-4">@if(!$user->is(auth()->user()))<form method="POST" action="{{ route('users.destroy',$user) }}" onsubmit="return confirm('حذف هذا الحساب؟')">@csrf @method('DELETE')<button class="rounded-lg bg-rose-50 px-3 py-1.5 text-xs font-bold text-rose-700">حذف</button></form>@else<span class="text-xs text-slate-400">حسابك</span>@endif</td></tr>@endforeach</tbody></table><div class="mt-5">{{ $users->links() }}</div></div></div></x-app-layout>
