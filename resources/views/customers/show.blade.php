<x-app-layout><div class="dashboard-shell px-4 py-6 md:px-8"><div class="mx-auto max-w-4xl space-y-6"><div class="panel p-6"><div class="flex items-center justify-between"><div><p class="text-sm text-slate-500">ملف العميل</p><h1 class="text-2xl font-bold">{{ $customer->name }}</h1></div><a href="{{ route('customers.edit', $customer) }}" class="rounded-xl bg-slate-900 px-4 py-2 text-sm font-semibold text-white">تعديل</a></div><div class="mt-6 grid gap-4 md:grid-cols-3"><p>الهاتف: {{ $customer->phone }}</p><p>المنطقة: {{ $customer->district?->name ?? 'غير محددة' }}</p><p>العنوان: {{ $customer->address ?? 'غير مضاف' }}</p></div></div><div class="panel p-6"><h2 class="text-lg font-bold">طلبات العميل</h2><div class="mt-4 space-y-3">@forelse($customer->orders as $order)<a href="{{ route('orders.show', $order) }}" class="flex justify-between rounded-xl border p-3"><span>{{ $order->order_number }}</span><span>{{ $order->status }} · {{ number_format($order->total, 0) }} ل.س</span></a>@empty<p class="text-slate-500">لا توجد طلبات.</p>@endforelse</div></div></div></div></x-app-layout>
